# Notebooks

The six notebooks were copied from the local `MACHINE LEARNING` folder and renamed for readability:

- `01_random_forest.ipynb`
- `02_extra_trees.ipynb`
- `03_hist_gradient_boosting.ipynb`
- `04_lightgbm.ipynb`
- `05_knn.ipynb`
- `06_support_vector_regressor.ipynb`

Each notebook now uses repository-relative paths, writes cleaned data to `outputs/ml/<algorithm>/`, writes models to `models/<algorithm>/`, and writes split indices to `splits/<algorithm>/`.
