# Scripts

Top-level project helper scripts can live here. The recovered RASPA helper scripts are kept separately under `raspa/scripts/legacy/` because they came from the original GCMC workspace and retain provenance-specific assumptions.
